/**
 * Feature flags — toggle experimental or in-progress UI features.
 * Change a flag here and rebuild to enable/disable the feature everywhere.
 */

/** Render the fixture detail as a full-screen PrimeNG Drawer instead of a centred Dialog. */
export const FIXTURE_DETAIL_DRAWER = true;
